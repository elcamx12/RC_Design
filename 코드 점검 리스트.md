# 코드 점검 리스트

점검 요청 시 Part A → B → C → D 순서로 수행.
각 항목에 ✅/❌/⚠️ 표시하고, ❌ 발견 시 즉시 수정 코드 제시.

---

## Part A: Python 코드 범용 점검 (50항목)

### A1. 크래시 유발 — 즉시 터지는 오류 (12항목)

| # | 점검 항목 |
|---|---|
| A1-1 | `.get()` 반환값 None에 바로 `.xxx()` 메서드 호출 → AttributeError |
| A1-2 | `float(None)`, `int(None)`, `float("")` → TypeError/ValueError |
| A1-3 | `dict['key']`에서 key 없음 → KeyError (`.get()` 사용해야) |
| A1-4 | 빈 리스트에 `[0]` 접근 → IndexError |
| A1-5 | 0으로 나누기 → ZeroDivisionError (d=0, h=0, L=0 등) |
| A1-6 | 음수에 sqrt → ValueError (판별식 음수, 면적 음수 등) |
| A1-7 | 반복문이 한번도 안 돌아서 변수 미정의 → NameError |
| A1-8 | import 실패 시 모듈 사용 → NameError (try/except import 후 분기 누락) |
| A1-9 | 파일 경로에 한글/공백 → 인코딩/경로 오류 |
| A1-10 | 튜플 언패킹 시 개수 불일치 → ValueError (`a, b, c = func()` 근데 2개만 반환) |
| A1-11 | numpy 배열 연산에서 shape 불일치 → broadcasting 오류 |
| A1-12 | 재귀 깊이 초과 / while True 탈출 못함 → RecursionError / 무한루프 |

### A2. 잘못된 결과 — 터지지 않지만 값이 틀림 (12항목)

| # | 점검 항목 |
|---|---|
| A2-1 | 변수명 오타로 잘못된 변수 참조 (copy-paste 후 수정 누락) |
| A2-2 | 부동소수점 `==` 비교 → 미세 오차로 조건 불일치 |
| A2-3 | `and`/`or` 우선순위 → 괄호 없이 `a or b and c` = `a or (b and c)` |
| A2-4 | `bool(0)`, `bool(0.0)`, `bool("")` = False → 유효한 값 0을 누락 처리 |
| A2-5 | off-by-one: `range(n)` vs `range(n+1)`, `<=` vs `<` |
| A2-6 | 같은 수식이 2곳에 복사됐는데 한쪽만 수정 → 불일치 |
| A2-7 | 단위 변환 실수 (×1000 해야 하는데 ÷1000, 또는 변환 누락) |
| A2-8 | 조건문에서 elif/else 빠져서 특정 경우만 처리 안 됨 |
| A2-9 | 정렬 순서 실수 (오름차순/내림차순 혼동) |
| A2-10 | 리스트 변경 중 순회 (for item in list → list.remove) → 건너뛰기 |
| A2-11 | 같은 dict/list를 여러 곳에서 참조 → 한쪽 수정이 다른쪽에 영향 (얕은 복사) |
| A2-12 | 함수 기본값으로 mutable 객체 (`def f(x=[])`) → 호출마다 누적 |

### A3. 데이터 흐름 오류 (8항목)

| # | 점검 항목 |
|---|---|
| A3-1 | 함수 반환값을 받지 않고 버림 (return 있는데 호출자가 `func()` 만 호출) |
| A3-2 | 전역 변수 의존 — 함수 호출 순서가 바뀌면 결과가 달라짐 |
| A3-3 | 같은 변수를 다른 함수에서 다른 의미로 사용 (h = 보 춤? 기둥 높이?) |
| A3-4 | 함수 파라미터 순서가 호출자와 불일치 (positional argument 실수) |
| A3-5 | 반환값 구조(dict, tuple)가 호출자의 기대와 불일치 |
| A3-6 | session_state에 저장한 값이 다음 rerun에서 예상과 다른 타입으로 존재 |
| A3-7 | 중간 계산 결과를 저장 안 해서 나중에 다시 계산할 때 값이 달라짐 |
| A3-8 | 여러 함수가 같은 session_state 키를 읽고 쓰면서 경합 발생 |

### A4. 예외 처리 결함 (6항목)

| # | 점검 항목 |
|---|---|
| A4-1 | `except Exception: pass` — 모든 에러를 삼켜서 원인 추적 불가 |
| A4-2 | except 안에서 잘못된 복구 로직 → 에러보다 나쁜 상태로 진행 |
| A4-3 | 외부 입력(PDF, API) 실패 시 None 반환 → 호출자가 None 체크 안 함 |
| A4-4 | 예외 발생 후 리소스(파일, 연결) 해제 안 됨 |
| A4-5 | API 타임아웃 처리 없음 → 무한 대기 |
| A4-6 | 사용자에게 보이는 에러 메시지가 기술적 traceback뿐 → 대응 방법 안내 없음 |

### A5. 성능/메모리 (5항목)

| # | 점검 항목 |
|---|---|
| A5-1 | Figure/큰 객체 생성 후 해제 안 함 → 메모리 누수 (plt.close 누락 등) |
| A5-2 | 동일 계산 반복 수행 (캐싱 가능한데 매번 재계산) |
| A5-3 | 불필요하게 큰 데이터를 session_state에 저장 → 메모리 증가 |
| A5-4 | 중첩 루프에서 안쪽에 무거운 연산 (I/O, API 호출 등) |
| A5-5 | 리스트 반복 생성/연결 대신 한번에 생성 가능한 곳 |

### A6. 가독성/유지보수 (7항목)

| # | 점검 항목 |
|---|---|
| A6-1 | 매직넘버 (40, 0.85, 1000 등) 설명 없이 사용 — 나중에 왜 이 값인지 모름 |
| A6-2 | 함수 50줄+ → 분리 필요 |
| A6-3 | 변수명 모호 (x, tmp, val, result) |
| A6-4 | 중복 코드 3곳 이상 → 함수로 추출 필요 |
| A6-5 | 주석이 코드와 불일치 (수정 후 주석 안 고침) |
| A6-6 | 사용하지 않는 import/변수/함수 존재 |
| A6-7 | TODO/FIXME/HACK 주석이 오래 방치됨 |

---

## Part B: RC_Design 프로젝트 범용 점검 (40항목)

### B1. 단위 변환 오류 — 계산 결과 자체가 틀려지는 치명적 항목 (10항목)

| # | 점검 항목 |
|---|---|
| B1-1 | 경간 mm→m 변환 누락/중복 (÷1000) |
| B1-2 | 모멘트 kN·m→N·mm 변환 누락/중복 (×1e6) |
| B1-3 | 전단력 kN→N 변환 누락/중복 (×1000) |
| B1-4 | 축력 kN→N 변환 누락/중복 (×1000) |
| B1-5 | MIDAS fck KPa→MPa 변환 누락 (÷1000) → fck=30000으로 계산되면 완전히 틀림 |
| B1-6 | Loc m→mm 변환 누락 (세로 텍스트 0.04 → 40mm) |
| B1-7 | 단위 주석과 실제 코드 불일치 ("# N/mm" 인데 실제 kN/m) |
| B1-8 | 반환값 단위가 호출자 기대와 불일치 (mm 반환인데 m로 사용) |
| B1-9 | 변환을 두 번 적용 (mm→m 한 값에 또 ÷1000) |
| B1-10 | cover(mm)와 Loc(mm)를 혼동하여 잘못된 d 계산 |

### B2. KDS 계산식 오류 — 구조 안전에 직결 (10항목)

| # | 점검 항목 |
|---|---|
| B2-1 | φ 값이 εt에 따라 변해야 하는데 고정값 사용 (전이구간 미반영) |
| B2-2 | 최소 철근비 ρ_min 검토 누락 → 실제보다 적은 철근으로 OK 판정 |
| B2-3 | 최대 철근비 / εt ≥ 0.005 검토 누락 → 취성 파괴 위험 |
| B2-4 | β1 = 0.85 고정 → fck > 28MPa에서 β1이 줄어야 하는데 안 줄어듦 |
| B2-5 | d 계산에서 cover/stirrup/rebar 반경 중 하나 누락 → d가 실제보다 큼 → 위험측 |
| B2-6 | 판별식(discriminant) 음수 → "단면 부족"인데 에러 처리 없이 As=0 반환 |
| B2-7 | P-M 상관도에서 Bresler 공식의 φ가 전이구간을 미반영 |
| B2-8 | 세장비 λ > 100 검토 → 설계 불가인데 경고 없이 진행 |
| B2-9 | 0.9D+E 하중조합에서 Pu_add의 D/L 분해 없이 전체 포함 |
| B2-10 | 지진하중 입력에서 음수 불가 (min_value=0) → 실제 음수 지진력 입력 못함 |

### B3. Streamlit/session_state 오류 (8항목)

| # | 점검 항목 |
|---|---|
| B3-1 | 위젯 key 충돌 → 같은 key를 다른 위젯이 사용하면 Streamlit 에러 |
| B3-2 | st.rerun() 무한루프 → 조건 미충족 시 계속 rerun |
| B3-3 | 모드 전환 시 이전 모드의 rv_/pdf_ 키가 새 모드에서 간섭 |
| B3-4 | number_input의 value가 session_state 기존값에 덮어씌워짐 → AI 분석 결과 무시 |
| B3-5 | multiselect 변경 → 이전 선택의 rv_ 키가 남아서 잘못된 값 사용 |
| B3-6 | plt.close() 누락 → Streamlit Cloud에서 메모리 부족 크래시 |
| B3-7 | session_state에 큰 객체(PDF bytes, Figure) 저장 → 메모리 폭증 |
| B3-8 | 설계 실행 버튼 클릭 시 입력값이 아직 갱신 안 된 상태에서 계산 시작 |

### B4. 엔진↔검토 모드 불일치 (6항목)

| # | 점검 항목 |
|---|---|
| B4-1 | KDS 수식이 엔진과 review에서 다른 버전 → 같은 입력에 다른 결과 |
| B4-2 | steps dict 키 이름 불일치 → output.py expander에서 KeyError |
| B4-3 | 설계 모드에서 수식 수정했는데 검토 모드는 구버전 유지 |
| B4-4 | review_inputs → results 변환에서 필드 누락 → output에서 None 참조 |
| B4-5 | 설계 모드의 rebar_detailing과 검토 모드의 _review_rebar 로직 차이 |
| B4-6 | 설계 모드의 OK/NG 판정 기준과 검토 모드의 판정 기준 불일치 |

### B5. 시각화 렌더링 오류 (6항목)

| # | 점검 항목 |
|---|---|
| B5-1 | 한글 폰트 없어서 텍스트 깨짐 (Streamlit Cloud = Linux) |
| B5-2 | set_aspect('equal') 누락 → 단면이 찌그러짐 |
| B5-3 | 철근 원 크기가 실제 직경과 불일치 → 시각적 오해 |
| B5-4 | Loc 치수가 cover+stirrup+rebar/2와 불일치 → 잘못된 단면도 |
| B5-5 | 늑근이 주근 외경에 안 맞음 → MIDAS 스타일 불일치 |
| B5-6 | END-I/MID/END-J 배근이 섞여서 잘못된 위치에 표시 |

---

## Part C: PDF 파서 전용 점검 (30항목)

### C1. 크래시 유발 (8항목)

| # | 점검 항목 |
|---|---|
| C1-1 | 빈 PDF → page.chars = [] → IndexError |
| C1-2 | 이미지만 있는 PDF → extract_text() = "" → 모든 _find_word 실패 → None 체인 |
| C1-3 | 암호화된 PDF → pdfplumber.open() 실패 → 예외 미처리 |
| C1-4 | 1000페이지 PDF → 메모리 부족 또는 타임아웃 |
| C1-5 | _vertical_values에서 chars가 비어있으면 → max() 에러 (빈 시퀀스) |
| C1-6 | _three_values_at_row에서 col_centers가 비어있으면 → 빈 튜플 반환 → 후속 언패킹 실패 |
| C1-7 | BeST.Steel 다중 페이지에서 마지막 페이지가 잘리면 → 불완전 파싱 |
| C1-8 | MIDAS와 BeST 페이지가 예상 순서가 아닐 때 → 잘못된 페이지 분류 |

### C2. 잘못된 값 추출 (10항목)

| # | 점검 항목 |
|---|---|
| C2-1 | y tolerance가 너무 크면 → 인접 행의 값을 합침 (69.02와 65.78이 같은 행으로) |
| C2-2 | y tolerance가 너무 작으면 → 같은 행을 분리 (B=720 행이 2행으로) |
| C2-3 | x tolerance가 너무 크면 → 다른 컬럼(END-I, MID)의 값을 잘못 매칭 |
| C2-4 | 세로 텍스트 "0.04"를 "40.0"으로 읽음 (읽기 방향 실수) |
| C2-5 | 부분매칭 "M" → "Moment" 잡아서 잘못된 행에서 값 추출 |
| C2-6 | 같은 라벨 중복 ("Factored Strength"가 Bending/Shear 양쪽에) → 첫번째만 잡아서 Shear 값이 Bending 것으로 |
| C2-7 | fck 행과 fy 행 혼동 (둘 다 N/mm2, "=" 포함) |
| C2-8 | Mu와 Tu 혼동 ("M = -69.0 kN·m, T = 0.0 kN·m" 에서 T를 잡음) |
| C2-9 | T형보 top/bot 분류 실패 → B=720을 웹 폭으로 사용 → 계산 완전히 틀림 |
| C2-10 | Loc 추출에서 보 춤(0.25)을 Loc(0.04)로 잡음 → cover가 210mm로 계산 |

### C3. 누락 (7항목)

| # | 점검 항목 |
|---|---|
| C3-1 | Mu_pos 미추출 (BeST는 단일값만 → pos 없음) → 검토 모드에서 pos 검토 누락 |
| C3-2 | Shear 하중조합 번호 미추출 → 지배조합 표시 불가 |
| C3-3 | Using Rebar (As.top/As.bot) 미추출 → MIDAS 배근면적 비교 불가 |
| C3-4 | BeST Vs,req 미추출 → 전단보강 비교 불가 |
| C3-5 | BeST Required Stirrup ("2-D10 @ 70") 미추출 |
| C3-6 | MIDAS 기둥 파서 미구현 → MIDAS 기둥 페이지 무시됨 |
| C3-7 | 부재명이 없는 페이지 → member="" → 다른 부재와 혼동 |

### C4. 안정성/호환성 (5항목)

| # | 점검 항목 |
|---|---|
| C4-1 | MIDAS Gen 버전 차이로 텍스트 형식 변경 → 기존 파서 깨짐 |
| C4-2 | BeST 버전 차이 (3.0 → 4.0) → 레이아웃 변경 → 좌표 기반 파서 깨짐 |
| C4-3 | 페이지 크기 A4 아닐 때 (Letter, A3) → 좌표 범위 다름 |
| C4-4 | 전각 숫자 (１２３) 처리 누락 → float 변환 실패 |
| C4-5 | PDF 내 폰트 임베딩 방식에 따라 pdfplumber 텍스트 추출 실패 |

---

## Part D: 비판적 사고 점검 (20항목)

### D1. "고의로 깨뜨리기" (7항목)

| # | 점검 항목 |
|---|---|
| D1-1 | **빈 PDF 업로드** → 크래시? 빈 결과? 에러 메시지? |
| D1-2 | **이미지만 있는 PDF** → 텍스트 추출 0 → AI에 빈 텍스트 전달 → 쓸모없는 결과 |
| D1-3 | **fck=0, fy=0 입력** → ZeroDivisionError? 음의 As? |
| D1-4 | **Mu=0 전 위치** → 최소 철근만? 아니면 As=0으로 빈 단면도? |
| D1-5 | **보 춤 h < cover** → d = 음수 → sqrt 음수 → ValueError |
| D1-6 | **같은 부재명 10개** → 어떤 게 선택되는가? 인덱스 충돌? |
| D1-7 | **설계 모드에서 검토 모드 session_state 키가 남아있을 때** → 잘못된 기본값 |

### D2. "실무에서 터질 것" (7항목)

| # | 점검 항목 |
|---|---|
| D2-1 | **MIDAS 2026 버전** → 텍스트 형식 소폭 변경 → 어떤 값이 먼저 깨지나? |
| D2-2 | **보 50개 + 기둥 100개 PDF** → 파싱 시간? UI 렌더링 성능? |
| D2-3 | **ACI/Eurocode 구조계산서** → MIDAS 형식으로 오분류 → 잘못된 값 추출 |
| D2-4 | **검토 결과 NG인데 사용자가 무시** → 경고가 눈에 충분히 띄는가? |
| D2-5 | **cover 값을 잘못 입력** (20mm인데 40mm) → φMn이 실제보다 작게 → 과다설계 |
| D2-6 | **PC 부재에 현장타설 cover 적용** → d가 작아져 불필요하게 NG 판정 |
| D2-7 | **검토비 > 1.0 결과를 보고서에 그대로 출력** → 검수자가 놓칠 수 있는 구조인가? |

### D3. "유지보수자가 겪을 고통" (6항목)

| # | 점검 항목 |
|---|---|
| D3-1 | **숨겨진 의존성** — A 수정하면 B가 깨지는데 코드에서 안 보임 |
| D3-2 | **에러 추적 불가** — 어디서 값이 잘못됐는지 로그/디버그 수단 없음 |
| D3-3 | **수정 영향 범위 파악 불가** — steps 키 하나 바꾸면 output.py 어디가 깨지나? |
| D3-4 | **테스트 없음** — KDS 수식 수정 후 결과가 맞는지 확인할 방법 없음 |
| D3-5 | **문서 불일치** — CLAUDE.md/계획 및 수정사항.md와 실제 코드가 다름 |
| D3-6 | **매직넘버 지옥** — 40, 0.85, 1000, 50 등이 설명 없이 산재 → 왜 이 값인지 모름 |

---

## 사용 방법

1. `점검해줘` → Part A~D 전체 수행
2. `PDF 파서 점검해줘` → Part A + C + D 수행
3. `검토 모드 점검해줘` → Part A + B + D 수행
4. `가볍게 점검해줘` → Part A1~A3 + D1만 수행
5. `실질적 오류만 점검해줘` → A1~A4 + B1~B2 + C1~C2 + D1~D2만 수행

---

## Part E: UI / 검토 모드 / Streamlit 전용 점검 (55항목)

### E1. Streamlit session_state 오류 (12항목)

| # | 점검 항목 |
|---|---|
| E1-1 | `st.session_state['key']` 직접 접근 → KeyError. `.get('key', default)` 사용해야 |
| E1-2 | 위젯 key와 session_state key 충돌 → DuplicateWidgetID |
| E1-3 | `st.rerun()` 무한 루프 — 조건 없이 호출하면 영구 리런 |
| E1-4 | 위젯 key 삭제 후 rerun → 위젯이 기본값으로 리셋되는지 확인 |
| E1-5 | 모드 전환(설계↔검토) 시 이전 모드의 session_state 잔류 → 잘못된 값 참조 |
| E1-6 | multiselect에서 선택 변경 시 하위 위젯 key가 갱신되지 않는 문제 |
| E1-7 | 같은 key를 여러 위젯에 사용 (다른 탭/expander에 있어도 충돌) |
| E1-8 | `st.number_input(value=X, key=K)` — session_state[K]가 이미 있으면 value 무시됨 |
| E1-9 | `st.text_input`에 float 넣고 float()로 변환 시 빈 문자열 → ValueError |
| E1-10 | PDF 재업로드 시 이전 분석 결과가 session_state에 잔류 → 혼합 데이터 |
| E1-11 | 검토 부재 선택 해제 시 해당 부재의 rv_ 키가 제대로 삭제되는지 |
| E1-12 | `st.button` 클릭 후 rerun되면 버튼 상태가 False로 리셋 → 로직 타이밍 문제 |

### E2. 검토 모드 입력 연결 오류 (10항목)

| # | 점검 항목 |
|---|---|
| E2-1 | AI 분석 결과 → 검토 입력 전달 시 키 이름 불일치 (Mu_neg_I_kNm vs Mu_neg) |
| E2-2 | 자동 분석 결과 → 검토 입력 전달 시 데이터 구조 불일치 (dict vs BeamResult) |
| E2-3 | 부재명 중복 시 (같은 이름 MIDAS+BeST) 어느 값이 적용되는지 모호 |
| E2-4 | fck/fy가 부재별로 다른데 올바른 부재의 fck/fy가 사용되는지 |
| E2-5 | cover 값이 Loc에서 역산된 건지, 기본값(40)인지 구분 가능한지 |
| E2-6 | 배근 문자열 "3-D19"가 _parse_rebar_string()으로 정확히 파싱되는지 |
| E2-7 | 기둥 bx/by 매핑 — AI는 Cx_mm/Cy_mm, 자동은 다른 키 → 통일 여부 |
| E2-8 | h_column 단위 — mm로 전달되는지 m로 전달되는지 확인 |
| E2-9 | 검토 입력에서 값 수정 후 "검토 실행" → 수정된 값이 실제 계산에 반영되는지 |
| E2-10 | 색상 인디케이터(빨강/노랑) 상태가 값 변경 시 정확히 갱신되는지 |

### E3. 검토 모드 계산 오류 (15항목)

| # | 점검 항목 |
|---|---|
| E3-1 | Mu=0 입력 시 → As_req=0 정상 반환? 아니면 에러? |
| E3-2 | Vu=0 입력 시 → 전단 계산 스킵? 아니면 s=∞? |
| E3-3 | h=0 또는 b=0 → ZeroDivisionError 방어 |
| E3-4 | fck=0 또는 fy=0 → 계산 불가 → 에러 메시지 표시하는지 |
| E3-5 | 판별식 음수 (단면 부족) → As_req=0 반환 + 경고 메시지 정확성 |
| E3-6 | 구조계산서 배근 기반 φMn 계산 시 d가 Loc 기반으로 정확히 계산되는지 |
| E3-7 | _calc_phi_Mn에서 εt 기반 φ 산정 — c 계산이 정확한지 |
| E3-8 | _parse_rebar_string("3-D19") → (3, 19.1, 286.5, 859.5) 정확성 |
| E3-9 | _parse_rebar_string("2-D10@125") 스터럽 형식도 처리하는지 |
| E3-10 | _parse_rebar_string("12EA-4R-D19") BeST 형식도 처리하는지 |
| E3-11 | 기둥 P-M 계산에서 Mu=√(Mux²+Muy²) SRSS 적용이 정확한지 |
| E3-12 | 기둥 세장비 λ=k·lu/r에서 h_column 단위 (mm 기대) 확인 |
| E3-13 | 기둥 EI 계산에서 Ig = c⁴/12 → c 단위 mm? → Ig 단위 mm⁴? |
| E3-14 | 기둥 Pc = π²EI/(klu)² 에서 단위 통일 (N, mm) |
| E3-15 | 보 전단 s_final이 음수나 0이 되는 케이스 방어 |

### E4. 검토 모드 출력 오류 (8항목)

| # | 점검 항목 |
|---|---|
| E4-1 | results['mode'] == 'review' 분기가 output.py에서 정확히 동작하는지 |
| E4-2 | 검토 결과 테이블에서 END-I/MID/END-J 컬럼 순서가 맞는지 |
| E4-3 | Check Ratio > 1.0이면 빨간색으로 표시하는지 |
| E4-4 | OK/NG 판정이 결과 dict의 ok_overall과 일치하는지 |
| E4-5 | 검토 불가 항목(처짐, IMF 등)이 "검토 불가" 메시지로 표시되는지 |
| E4-6 | P-M 다이어그램에 설계점이 올바른 위치에 표시되는지 |
| E4-7 | 단면도에 구조계산서 배근이 표시되는지 (As_provided 기반) |
| E4-8 | plt.close(fig) 누락 → 메모리 누수 확인 |

### E5. UI 레이아웃 / 표시 오류 (10항목)

| # | 점검 항목 |
|---|---|
| E5-1 | 사이드바 입력 위젯 → 메인 영역 결과 연동이 정확한지 |
| E5-2 | 탭(설계결과/시각화/보고서) 전환 시 데이터 손실 없는지 |
| E5-3 | 모드 전환 시 UI가 깨지지 않는지 (설계→검토→설계) |
| E5-4 | st.metric 값이 잘리는 문제 (...으로 표시) — 컬럼 폭 확인 |
| E5-5 | 한글 폰트 깨짐 — Matplotlib에서 NanumGothic 설정 확인 |
| E5-6 | 숫자 입력 필드에 문자 입력 시 → ValueError 방어 |
| E5-7 | 색상 인디케이터 CSS가 다른 위젯에 영향 주지 않는지 |
| E5-8 | 보고서 탭에서 검토 모드 결과가 반영되는지 (또는 미지원 안내) |
| E5-9 | 지중보 섹션이 검토 모드에서 불필요하게 표시되지 않는지 |
| E5-10 | expander 열기/닫기 상태가 rerun 후에도 유지되는지 |

---

## 점검 명령어

점검 결과는 아래 형식으로 보고:

```
### 점검 결과 요약
- ✅ 통과: 85/105
- ⚠️ 주의: 12/105 (개선 권장)
- ❌ 실패: 8/105 (즉시 수정 필요)

### ❌ 실패 항목 (심각도 순)
| # | 항목 | 발견 내용 | 위치 | 수정 방안 |
|---|---|---|---|---|
| B2-6 | 판별식 음수 | As=0 반환, 에러 없음 | review:L45 | NG 판정 + 경고 |
| A1-5 | 0 나누기 | h_column=0 → ZeroDivision | review:L533 | if h<=0: 기본값 |
| C2-9 | T형보 오분류 | B=720 사용 → φMn 과대 | parser:L598 | top/bot 라벨 검증 |
```
