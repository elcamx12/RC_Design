# 패치노트

---

## v0.6.6 — 2026-03-17

### 계산 로직 개선

**3/17**
- (#63) 전단 Vs_max 초과 시 수렴 루프에서 b 또는 h 자동 증가
- (#64) 구간별 스터럽 간격 차등 적용 — stirrup_zone_summary (예: "D10@100/200/200")
- (#58) 세장비 Mu 증폭 방향별 독립 적용 — Mux, Muy 각각 delta_ns 증폭 후 SRSS 재조합
- (#43) 기둥 지배조합 — Pu 최대 → 각 조합별 P-M 예비 검토 후 최불리 자동 선택
- 부분 고정 시 수렴 루프에서 고정 부재 보호 (h만/b만/둘다 고정 각각 대응)

**3/16**
- (#5) 전단 위험단면 적용 — V_at_d = V_max - w_u·d (KDS 41 20 22)
- (#7) 기둥 하중조합 다양화 — 1.4D / 1.2D+1.6L / 1.2D+1.0E+1.0L / 0.9D+1.0E
- (#9) 보-기둥 접합부 전단 검토 — Vj = T - V_col, OMF/IMF γ 계수 (KDS 41 17)
- (#12) 기둥 이축 휨 Bresler 역수하중법 — 1/Pn = 1/Pnx + 1/Pny - 1/Pno
- (#14) IMF 내진 상세 — 보 소성힌지, 기둥 구속구간, 강기둥-약보 검토
- (#27) 기둥 beta_d 가변 계산 — 고정(0.6) → 1.2D/(1.2D+1.6L)
- (P6) 정착길이 cb 수정 — cb = min(피복거리, 철근간격/2)
- (P7) 기둥 모멘트 조합 — SRSS → Bresler Method

### UI 개선

**3/17**
- 입력 순서 교체: 2-1(바닥보 설계) → 2-2(기둥별 추가 하중)
- "4. 부재 사이즈 지정" 섹션 제거 → "1. 자동결정된 단면"으로 통합
- `st.toggle("자동 (수렴설계)")` 추가 — 부재별 개별 추적, 수정 시 🟡 표시
- 슬래브/보/기둥 설계 섹션에 st.expander 접기/펼치기 적용
- 패치노트 UI: 최상단 접기/펼치기 섹션, 목~수 주간 단위 탭 그룹핑

**3/16**
- 출력 섹션 전면 재배치: 10개 독립 함수, 부재 유형별 그룹핑
- 슬래브 계산 상세 expander 4개 (휨/전단/처짐/배근)
- 기둥 하중분해 + 하중조합 상세 표
- IMF 내진상세 / 균열 제어 / 정착길이 계산 공식 expander
- 입력 시스템 리팩토링: 부재 사이즈 직접 지정 + PDF 파서 연동 + AI→RC 설계 적용
- "바닥보 설계 포함" 토글 추가
- 내장 API 키 + 로그인 시스템

### 구조계산서 보고서

**3/17**
- `report_generator.py` 신규: HTML 구조계산서 생성 (표지, 목차, 6개 섹션)
- A4 인쇄 최적화 CSS + 새 창 인쇄 다이얼로그로 PDF 저장
- 상세 내용은 초기 틀만 구성, 추후 채움 예정

### 보안

**3/17**
- (S1) API 키/비밀번호 하드코딩 제거 → `.streamlit/secrets.toml` + `st.secrets` 방식으로 이전

### 버그 수정

**3/17**
- AI 분석 결과가 부재 선택 시 사라지는 버그 — session_state 저장으로 해결
- 보고서 출력 시 한글 폰트 깨짐 — atob → Uint8Array 변환
- 비수정 부재의 override 값이 갱신되지 않던 문제 — 매 계산 후 자동값으로 갱신
- JavaScript CSS 입력칸 하이라이트 렌더링 지연 → 제거 (🟡 이모지만 유지)

**3/16 — 코드 감사 (23건)**
- **B등급 (6건)**: 0.9D+1.0E 조합 하중 누락(#57), 배력근 As d→t_slab(#49), 균열 cc 스터럽 누락(#47), mu_breakdown 자중 수렴 갱신(#59), AFD/BMD 기둥 좌표 통일(#63), 슬래브 최소 철근비 보 기준 제거(#54)
- **C등급 (6건)**: P-M 판정 하드코딩→조건부(#52), 슬래브 처짐 d_b 실제값(#56), 스터럽 구간별 시각화(#53), P-M 인장근 주석(#41), 바닥보 오버라이드 전달(#61), 접합부 V_col 주석(#44)
- **D등급 (6건)**: 슬래브 최소두께 경계조건별(#29), size_fixed 주석(#45), Pu_add 계수 문서화(#60), 2단 배근 d_c 보정(#24), 장변보 분담폭 0 방지(#42), 슬래브 Icr 지점/중앙 분리(#55)
- **A등급 (5건)**: KDS 조항번호 정정(#46), 보 자중 음수 방지(#51), 전단 경고 변수(#50), P-M 라벨 φ 추가(#64), 기둥 자중 수렴 경고(#62)

### 시각화

**3/16**
- P-M φ 전이구간 가시화 — c 스윕 시작점 0.80Pno 경계로 변경
- 보 스터럽 대칭 미러링 수정
- 3D 원기둥 해상도 통일 (8면체 → 12면체)
- 바닥보 데이터 없을 시 천장보 폴백 처리

### 코드 구조

**3/16**
- `ui_renderer.py` → `ui/` 폴더 분리 (input, output, pdf_import)
- `visualization.py` → `visualization/` 폴더 분리 (beam, column, slab, frame_3d, helpers)
- AI 파서 모델 업데이트: claude-3-5-haiku → claude-haiku-4-5

### 배포

**3/16**
- GitHub 리포지토리: `https://github.com/elcamx12/RC_Design`
- Streamlit Community Cloud 배포: [rcdesign-kobwmhmwgee5w3gh9gcjwf.streamlit.app](https://rcdesign-kobwmhmwgee5w3gh9gcjwf.streamlit.app/)

### 문서

**3/17**
- `계획.md` 구조 개편: 균열검토, 정착길이, 처짐검토, IMF 상세, 슬래브 설계 문서화
- `계획.md` 해결된 이슈 제거 (#43, #58, #63, #64) + #18 보고서 "구체화 필요"로 수정
- `계획.md` 코드 감사 누락항목 6건 추가 (#41 Bresler φ 전이구간, #42 0.9D+1.0E Pu_add 분해, #43 단위주석 오류, #44 지진력 음수입력, #45 보고서 .get() 안전장치, #46 보고서 다중기둥)

---

## v0.5 — 2026-03-11

### 계산 로직

**3/11**
- KDS 14 20 → KDS 41 20 00 (건축물 콘크리트구조 설계기준) 전환 완료
- 전체 코드 감사 실시 (로직/코드 오류, KDS 불일치, 시각화 오류, 실무 관행 정리)

**3/10**
- 강도감소계수 φ 수렴 루프 — ε_t 기반 전이구간 보간 반복 수렴
- 보 주근 탐색 범위 확대 — D16 → D13부터 탐색
- 기둥 최소 단면 제거 — 400mm 하한 제거, 세장비·P-M으로 결정
- 균열제어 검토 추가 (KDS 14 20 50)
- 정착길이/이음길이 추가 (KDS 14 20 52)
- 2방향 슬래브 하중분배 — 사다리꼴/삼각형 등가 분담폭
- 바닥보 독립 설계 — 천장보와 별도 BeamAnalyzer

**3/9 이전**
- 보 처짐 수렴 루프 (Branson Ie, 양단 고정 가중평균)
- 기둥 P-M 상관도 3점 포락선 (A/B/C, 이분법 60회)
- 슬래브 1방향 설계 (1m 스트립, 고정단)
- Streamlit UI 기본 구조, Plotly 3D 배근도
- PDF 파서 모듈 (parsers/)
- BAT 런처 (RC_Design.bat)
